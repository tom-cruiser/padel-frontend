'use client';'use client';



import { useState, useEffect } from 'react';import { useState, useEffect } from 'react';

import { socketService, type OnlineUser } from '@/lib/socket';import { socketService, type OnlineUser } from '@/lib/socket';

import { useAuth } from '@/contexts/AuthContext';import { useAuth } from '@/contexts/AuthContext';

import ChatWindow from './ChatWindow';import ChatWindow from './ChatWindow';



export default function ChatList() {export default function ChatList() {

  const { user } = useAuth();  const { user } = useAuth();

  const [onlineUsers, setOnlineUsers] = useState<OnlineUser[]>([]);  const [onlineUsers, setOnlineUsers] = useState<OnlineUser[]>([]);

  const [allUsers, setAllUsers] = useState<OnlineUser[]>([]);  const [allUsers, setAllUsers] = useState<OnlineUser[]>([]);

  const [selectedUser, setSelectedUser] = useState<OnlineUser | null>(null);  const [selectedUser, setSelectedUser] = useState<OnlineUser | null>(null);

  const [isOpen, setIsOpen] = useState(false);  const [isOpen, setIsOpen] = useState(false);

  const [unreadCount, setUnreadCount] = useState(0);

  useEffect(() => {

  useEffect(() => {    if (user) {

    if (user) {      // Listen for online users updates

      // Listen for online users updates      socketService.onOnlineUsers((users) => {

      socketService.onOnlineUsers((users) => {        // Filter out current user

        // Filter out current user        const otherUsers = users.filter((u) => u.userId !== user.id);

        const otherUsers = users.filter((u) => u.userId !== user.id);        setOnlineUsers(otherUsers);

        setOnlineUsers(otherUsers);      });

      });

      // Fetch all users (this should be implemented in your API)

      // Fetch all users      const fetchAllUsers = async () => {

      const fetchAllUsers = async () => {        try {

        try {          const response = await fetch('/api/users');

          const response = await fetch('/api/users');          const users = await response.json();

          const users = await response.json();          setAllUsers(users.filter((u: any) => u.userId !== user.id));

          setAllUsers(users.filter((u: any) => u.userId !== user.id));        } catch (error) {

        } catch (error) {          console.error('Failed to fetch users:', error);

          console.error('Failed to fetch users:', error);        }

        }      };

      };      

      fetchAllUsers();

      // Fetch unread message count    }

      const fetchUnreadCount = async () => {  }, [user]);

        try {

          const response = await fetch('/api/messages/unread-count');  const handleStartChat = (recipient: OnlineUser) => {

          const data = await response.json();    setSelectedUser(recipient);

          setUnreadCount(data.count);  };

        } catch (error) {

          console.error('Failed to fetch unread count:', error);  const handleCloseChat = () => {

        }    setSelectedUser(null);

      };  };

      

      fetchAllUsers();  const toggleChatList = () => {

      fetchUnreadCount();    setIsOpen(!isOpen);

  };

      // Listen for new messages to update unread count

      socketService.onMessage(() => {  return (

        fetchUnreadCount();    <>

      });      {/* Floating Chat Button */}

    }      <button

  }, [user]);        onClick={toggleChatList}

        className="fixed bottom-4 left-4 w-14 h-14 bg-primary-600 text-white rounded-full shadow-lg hover:bg-primary-700 transition-all z-40 flex items-center justify-center"

  const handleStartChat = (recipient: OnlineUser) => {        title="Open chat"

    setSelectedUser(recipient);      >

  };        <svg className="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">

          <path

  const handleCloseChat = () => {            strokeLinecap="round"

    setSelectedUser(null);            strokeLinejoin="round"

  };            strokeWidth={2}

            d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"

  const toggleChatList = () => {          />

    setIsOpen(!isOpen);        </svg>

  };        {onlineUsers.length > 0 && (

          <span className="absolute -top-1 -right-1 bg-green-500 text-white text-xs font-bold rounded-full w-5 h-5 flex items-center justify-center">

  return (            {onlineUsers.length}

    <>          </span>

      {/* Floating Chat Button */}        )}

      <button      </button>

        onClick={toggleChatList}

        className="fixed bottom-4 left-4 pl-4 pr-6 py-3 bg-primary-600 text-white rounded-full shadow-lg hover:bg-primary-700 transition-all z-40 flex items-center gap-2 group"      {/* Chat User List */}

        title="Open messages"      {isOpen && (

      >        <div className="fixed bottom-20 left-4 w-96 bg-white rounded-lg shadow-2xl z-40 border border-gray-200">

        <svg className="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">          {/* Header */}

          <path          <div className="bg-primary-600 text-white px-4 py-3 rounded-t-lg flex items-center justify-between">

            strokeLinecap="round"            <h3 className="font-semibold">💬 Message Players</h3>

            strokeLinejoin="round"            <button

            strokeWidth={2}              onClick={toggleChatList}

            d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"              className="text-white hover:text-gray-200 transition-colors"

          />              aria-label="Close"

        </svg>            >

        <span className="font-medium">Messages</span>              <svg className="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">

        {(unreadCount > 0 || onlineUsers.length > 0) && (                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" />

          <div className="absolute -top-1 -right-1 flex items-center gap-1">              </svg>

            {unreadCount > 0 && (            </button>

              <span className="bg-red-500 text-white text-xs font-bold rounded-full w-5 h-5 flex items-center justify-center">          </div>

                {unreadCount}

              </span>          {/* Helpful Message */}

            )}          <div className="px-4 py-2 bg-primary-50 text-sm text-primary-700 border-b border-primary-100">

            {onlineUsers.length > 0 && (            Send messages to any player - they'll receive them when they come online!

              <span className="bg-green-500 text-white text-xs font-bold rounded-full w-5 h-5 flex items-center justify-center">          </div>

                {onlineUsers.length}

              </span>          {/* Users List */}

            )}          <div className="max-h-96 overflow-y-auto">

          </div>            {allUsers.length === 0 ? (

        )}              <div className="p-6 text-center">

      </button>                <p className="text-gray-500 text-sm">No other users found</p>

              </div>

      {/* Chat User List */}            ) : (

      {isOpen && (              <div className="divide-y divide-gray-100">

        <div className="fixed bottom-20 left-4 w-96 bg-white rounded-lg shadow-2xl z-40 border border-gray-200">                {allUsers.map((user) => {

          {/* Header */}                  const isOnline = onlineUsers.some(u => u.userId === user.userId);

          <div className="bg-primary-600 text-white px-4 py-3 rounded-t-lg flex items-center justify-between">                  return (

            <h3 className="font-semibold">💬 Message Players</h3>                  <button

            <button                    key={onlineUser.userId}

              onClick={toggleChatList}                    onClick={() => {

              className="text-white hover:text-gray-200 transition-colors"                      handleStartChat(onlineUser);

              aria-label="Close"                      setIsOpen(false);

            >                    }}

              <svg className="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">                    className="w-full px-4 py-3 hover:bg-gray-50 transition-colors text-left flex items-center gap-3"

                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" />                  >

              </svg>                    <div className="w-10 h-10 bg-primary-100 text-primary-700 rounded-full flex items-center justify-center font-semibold">

            </button>                      {onlineUser.firstName[0]}{onlineUser.lastName[0]}

          </div>                    </div>

                    <div className="flex-1">

          {/* Helpful Message */}                      <div className="flex items-center gap-2">

          <div className="px-4 py-2 bg-primary-50 text-sm text-primary-700 border-b border-primary-100">                        <div className="w-2 h-2 rounded-full bg-green-500"></div>

            Send messages to any player - they'll receive them when they come online!                        <p className="font-medium text-gray-900">

          </div>                          {onlineUser.firstName} {onlineUser.lastName}

                        </p>

          {/* Users List */}                      </div>

          <div className="max-h-96 overflow-y-auto">                      <p className="text-xs text-gray-600">{onlineUser.role}</p>

            {allUsers.length === 0 ? (                    </div>

              <div className="p-6 text-center">                    <svg className="w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">

                <p className="text-gray-500 text-sm">No other users found</p>                      <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 5l7 7-7 7" />

              </div>                    </svg>

            ) : (                  </button>

              <div className="divide-y divide-gray-100">                ))}

                {allUsers.map((userData) => {              </div>

                  const isOnline = onlineUsers.some(u => u.userId === userData.userId);            )}

                  return (          </div>

                    <button        </div>

                      key={userData.userId}      )}

                      onClick={() => {

                        handleStartChat(userData);      {/* Chat Window */}

                        setIsOpen(false);      {selectedUser && (

                      }}        <ChatWindow recipient={selectedUser} onClose={handleCloseChat} />

                      className="w-full px-4 py-3 hover:bg-gray-50 transition-colors text-left flex items-center gap-3"      )}

                    >    </>

                      <div className="w-10 h-10 bg-primary-100 text-primary-700 rounded-full flex items-center justify-center font-semibold">  );

                        {userData.firstName[0]}{userData.lastName[0]}}

                      </div>
                      <div className="flex-1">
                        <div className="flex items-center gap-2">
                          <div className={`w-2 h-2 rounded-full ${isOnline ? 'bg-green-500' : 'bg-gray-300'}`}></div>
                          <p className="font-medium text-gray-900">
                            {userData.firstName} {userData.lastName}
                          </p>
                        </div>
                        <div className="flex items-center gap-2">
                          <p className="text-xs text-gray-600">{userData.role}</p>
                          {!isOnline && (
                            <span className="text-xs text-gray-500">(Offline - will receive message later)</span>
                          )}
                        </div>
                      </div>
                      <svg className="w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 5l7 7-7 7" />
                      </svg>
                    </button>
                  );
                })}
              </div>
            )}
          </div>
        </div>
      )}

      {/* Chat Window */}
      {selectedUser && (
        <ChatWindow recipient={selectedUser} onClose={handleCloseChat} />
      )}
    </>
  );
}