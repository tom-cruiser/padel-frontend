// Navigation.tsx has been deprecated — Navbar component is used instead.
// Keeping a tiny stub to avoid accidental import runtime errors while code is migrated.

export default function Navigation() {
  return null;
}
